If you have not previously cloned the INDI 3rdparty sources for directions refer to section 3 in
https://indilib.org/forum/general/210-howto-building-latest-libindi-ekos.html.
This also specifies the apt packages that might be needed to build.

After completing the clone command to fetch the 3rdpary sources.
Using the example in the directions of ~/Project as the base:

$ cd ~/Project/indi-3rdparty
$ mkdir indi-rolloffino

Copy the rolloffino files into ~/Project/indi-3rdparty/indi-rolloffino

$ mkdir ~/Project/indi-3rdparty/indi-rolloffino/build
$ cd ~/Project/indi-3rdparty/indi-rolloffino/build
$ cmake -DCMAKE_INSTALL_PREFIX=/usr ../
$ make 
$ sudo cp rolloffino /usr/bin/


